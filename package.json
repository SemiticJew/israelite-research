{
  "name": "israelite-research",
  "private": true,
  "scripts": {
    "validate:dict": "node tools/validate-dictionary.mjs",
    "cap:build": "bash tools/build-capacitor-web.sh",
    "cap:sync:ios": "npm run cap:build && npx cap sync ios",
    "cap:open:ios": "npm run cap:sync:ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  }
}
