<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1" name="viewport"/>
<title>Who Are the “Gentiles”? — Semitic Jew</title>
<link href="/israelite-research/styles.css" rel="stylesheet"/>
<style>
    :root{ --accent:#F17300; --muted:#666; --ink:#0b2340; }
    .article-shell{ max-width:760px; margin:0 auto; }
    .article-head{ text-align:center; margin-top:.5rem; }
    .article-date{ color:var(--muted); font-size:.95rem; letter-spacing:.2px; margin-bottom:.35rem; }
    .article-title{ font-family:var(--ff-serif,"Adobe Garamond Pro",serif);
      font-size:clamp(1.9rem,3.4vw,2.6rem); color: var(--ink); line-height:1.15; margin:0 0 .8rem; }

    /* Article image (not a page hero): fixed 700×394, squared corners */
    .hero-image{ margin:1rem auto; }
    .hero-image img{ width:100%; height:auto; display:block; border-radius:0; }

    /* Byline */
    .article-byline{ display:flex; align-items:center; gap:.65rem; margin:.35rem 0 1.2rem; color: var(--ink); }
    .article-byline .author-avatar{ width:40px; height:40px; border-radius:50%; object-fit:cover;
      border:1px solid var(--border); background:#f0f3f8; }
    .byline-name{ font-weight:700; }
    .byline-meta{ color: var(--muted); font-size:.95rem; }

    /* Share */
    .article-share{ display:flex; align-items:center; gap:.5rem; margin:.2rem 0 1.25rem; }
    .share-btn{ display:inline-flex; align-items:center; justify-content:center; width:36px; height:36px;
      border-radius:999px; border:1px solid var(--border); background: var(--surface); color: var(--brand); text-decoration:none; }
    .share-btn:hover{ transform:translateY(-1px); }

    /* Body */
    .article-content{ color:#1d1d1d; font-size:1.06rem; line-height:1.7; }
    .article-content p{ margin:1rem 0; }
    .article-content h2{ font-family:var(--ff-serif); font-size:1.35rem; margin:1.6rem 0 .6rem; color: var(--ink); }
    .article-content blockquote{ border-left:4px solid var(--accent); padding:.25rem 1rem; color: var(--ink);
      background:#f9fbfe; border-radius:8px; }

    /* Verse hover “plugin” (articles-only) */
    .verse{ position:relative; text-decoration:underline dotted; color: var(--brand); }
    .verse:hover::after{
      content:"Open in Logos";
      position:absolute; left:0; bottom:120%;
      background: var(--surface); color: var(--brand); border:1px solid var(--border); padding:.25rem .45rem;
      font-size:.8rem; border-radius:8px; white-space:nowrap; box-shadow:0 4px 12px rgba(0,0,0,.08);
    }
  </style>
<style>.verse{position:relative;text-decoration:underline dotted;cursor:help;color:var(--brand);font-weight:600;}
.verse::after{content: attr(data-ref) " — " attr(data-note); position:absolute; left:0; bottom:120%; background:#ffffff; color:var(--ink); border:1px solid var(--border); padding:.45rem .6rem; font-size:.85rem; line-height:1.35; border-radius:8px; white-space:normal; width:min(360px,85vw); box-shadow:0 8px 20px rgba(0,0,0,.12); opacity:0; transform:translateY(4px); pointer-events:none; transition:opacity .12s ease, transform .12s ease; z-index:30;}
.verse:hover::after{opacity:1; transform:translateY(0);}</style><style>.verse{position:relative;text-decoration:underline dotted;cursor:help;color:var(--brand);font-weight:600;}
.verse::after{content: attr(data-ref) " — " attr(data-note); position:absolute; left:0; bottom:120%; background:#ffffff; color:var(--ink); border:1px solid var(--border); padding:.45rem .6rem; font-size:.85rem; line-height:1.35; border-radius:8px; white-space:normal; width:min(360px,85vw); box-shadow:0 8px 20px rgba(0,0,0,.12); opacity:0; transform:translateY(4px); pointer-events:none; transition:opacity .12s ease, transform .12s ease; z-index:30;}
.verse:hover::after{opacity:1; transform:translateY(0);}</style></head>
<body class="article-doc article-doc" data-page="article">
<!-- Header partial -->
<div id="site-header"></div>
<main class="container article-page">
<!-- Breadcrumbs -->
</main><script defer="True" src="/israelite-research/js/verses.js"></script></body></html>